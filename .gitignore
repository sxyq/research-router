.DS_Store

# Route, feedback, summary, and experience data stay local to the Skill runtime.
records/routes/**/*.json
records/feedback/*.json
records/summaries/*.json
records/experience/**/*.jsonl

# The Agent's weekly update check state is local to each Skill installation.
.research-router-update-state.json
